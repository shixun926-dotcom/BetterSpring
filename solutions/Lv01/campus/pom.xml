<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!--
      ① parent —— 版本仲裁中心
      spring-boot-starter-parent 的 parent 又是 spring-boot-dependencies，
      那里用 <dependencyManagement> 预先声明了几乎所有常用依赖的版本。
      所以下面 <dependencies> 里的 spring-boot-starter-web 才能"不写版本号"。
    -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.16</version>
        <relativePath/>
    </parent>

    <groupId>com.campus</groupId>
    <artifactId>campus</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>campus</name>
    <description>校园综合管理系统</description>

    <properties>
        <!--
          ② java.version —— 编译与运行的目标 Java 版本。
          parent 会把它转成 maven.compiler.release，最终作用于 javac 的 release 参数。
        -->
        <java.version>17</java.version>
    </properties>

    <dependencies>
        <!--
          ③ Starter —— 一组预置依赖的打包入口。
          spring-boot-starter-web 实际拉进来：spring-web / spring-webmvc /
          内嵌 tomcat / jackson / validation 等一整串，且版本互相兼容。
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- ④ 把项目打成可执行 jar（内含内嵌 Tomcat），java -jar 即可启动 -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
